//Visualforce
# 📝 Visualforce Detailed Notes (Based on your Content)

### 1. Introduction to Visualforce (The "What" and "How")

* **Definition:** Visualforce ek **web development framework** hai jo developers ko Lightning Platform par host hone wali mobile aur desktop apps ke liye **sophisticated, custom user interfaces** banane ki power deta hai.
* **Styling:** Aap isse Lightning Experience jaisa look bhi de sakte hain ya apna ekdum **completely custom interface** bhi bana sakte hain.
* **Flexibility:** Ye Salesforce ke built-in features ko **extend** (badhane), naya functionality se **replace** karne, ya ekdum **new apps** banane ke kaam aata hai.
* **Server Processing:** Jab koi page access karta hai, toh **server data processing** karta hai, page ko HTML mein render karta hai, aur fir browser ko result bhejta hai.

### 2. An Example Visualforce Page (Code Analysis)

Aapke content ka example code dikhata hai ki kaise **dozen lines** mein bahut saara kaam ho jata hai:

* **Standard Controller Connection:** Bina code likhe ye automatic data access aur modification provide karta hai.
* **Smart Behavior:**
* **New vs Edit:** Agar record ID nahi hai, toh blank form dikhayega. Agar ID hai, toh data lookup karke editable form dikhayega.
* **Field Intelligence:** Email field valid email check karti hai aur date field mein apne aap **date widget** (calendar) aa jata hai.
* **Standard Actions:** `Save` button direct controller ke `save` method ko call karta hai.



### 3. Where You Can Use Visualforce (6 Key Places)

Aapke content ke mutabiq, in 6 jagaho par Visualforce use hota hai:

1. **App Launcher:** Custom apps aur tabs ke roop mein (View All mein milta hai).
2. **Navigation Bar:** App ke navigation items mein Visualforce tabs add kar sakte hain.
3. **Standard Page Layout:** Record ke **Details** section mein custom content embed karne ke liye.
4. **Lightning App Builder:** Iske liye page par **"Available for Lightning Experience, Lightning Communities, and the mobile app"** checkbox enable hona zaroori hai.
5. **Quick Action:** Publisher area mein quick action ki tarah launch karna.
6. **Standard Button Overrides:** Jab user "New" ya "Edit" button click kare, toh standard page ki jagah aapka Visualforce page dikhe.

---

### 🚀 Code Snippet for Your Notes

Aap ye code apne notes mein rakhein, isme har line ka matlab likha hai:

```html
<apex:page standardController="Contact"> 
    <apex:form> 
        <apex:pageBlock title="Edit Contact"> 
            <apex:pageBlockSection columns="1"> 
                <apex:inputField value="{!Contact.FirstName}"/>
                <apex:inputField value="{!Contact.LastName}"/>
                <apex:inputField value="{!Contact.Email}"/>
                <apex:inputField value="{!Contact.Birthdate}"/>
            </apex:pageBlockSection> 
            <apex:pageBlockButtons> 
                <apex:commandButton action="{!save}" value="Save"/> 
            </apex:pageBlockButtons> 
        </apex:pageBlock> 
    </apex:form>
</apex:page>

```

---

### 🎯 Quick Summary for Quiz

* **Visualforce is:** A web development framework for custom UIs on Lightning Platform.
* **Display locations:** Sab jagah ho sakta hai (Tabs, Layouts, Buttons), **except "Any Setup Page"**.
* **Integration:** Ye kisi bhi standard web technology ya **JavaScript framework** ke saath integrate ho sakta hai.

Cool 👍
Neeche **poora content Hinglish me**, **step-by-step**, **without skipping anything**, **notes banane ke format me** diya hai. Tum directly exam / revision / Trailhead ke liye use kar sakte ho.

---

# 📘 Create & Edit Visualforce Pages – Hinglish Notes

---

## 🎯 Learning Objectives (Is unit ke baad tum kya seekh loge)

Is unit complete karne ke baad tum:

* Samajh paoge **Visualforce Page kya hota hai** aur uske **key attributes** kya hote hain
* Apni Salesforce org me **existing Visualforce pages ko list aur open** kar paoge
* **Developer Console** ka use karke **Visualforce page create aur edit** kar paoge
* **Visualforce tags aur attributes** ko identify, add aur customize kar paoge

---

## 🔰 Introduction to Visualforce Pages

* **Visualforce pages** application developers ke liye **basic building blocks** hote hain
* Ye **normal web pages jaise** hote hain, but extra power ke saath:

  * Salesforce data ko **access**, **display**, aur **update** kar sakte hain
* Har Visualforce page ka **unique URL** hota hai

  * Bilkul traditional web server pages jaise

---

## 🏷️ Visualforce Markup Language

* Visualforce ek **tag-based markup language** use karta hai (HTML jaisa)
* Har **Visualforce tag** ek UI component ko represent karta hai:

  * Page section
  * List view
  * Individual field
* Salesforce me lagbhag **150+ built-in Visualforce components** hote hain
* Developers apne **custom components** bhi bana sakte hain
* Visualforce ke saath:

  * HTML
  * CSS
  * JavaScript
    freely mix kar sakte ho → full UI flexibility 💡

---

## 🛠️ Visualforce Pages Create/Edit Karne ke Tarike

Visualforce pages ko multiple ways se create/edit kar sakte ho:

1. **Developer Console** ✅ (Best option)
2. **Setup Editor**
3. **Development Mode footer**
4. **Salesforce APIs**
5. **Visual Studio Code (VS Code)** with Salesforce Extensions

---

## 💻 Create Visualforce Page Using Developer Console

### 🔹 Developer Console Kya Hai?

* Salesforce ka **most powerful built-in development tool**
* Features:

  * Syntax highlighting
  * Auto-complete
  * Auto-suggest
  * Tag matching
  * Smart indenting

👉 Complex pages aur custom controllers ke liye **best tool**

---

### 🪜 Steps to Create Visualforce Page

1. Salesforce me **Your Name** ya **Setup gear icon** se
   👉 **Developer Console open karo**
2. New window open hogi
3. Click:
   **File | New | Visualforce Page**
4. Page name enter karo:
   **HelloWorld**
5. Click **OK**
6. Blank Visualforce page open ho jayega

---

### ✍️ Basic Visualforce Code

```html
<apex:page>
    <h1>Hello World</h1>
</apex:page>
```

7. **File | Save** karo
8. **Preview** button click karo

   * New window me page render hoga
   * Salesforce styling nahi dikhegi

---

### ⚡ Lightning Experience me Page Open Karna

* Lightning me direct `/apex/PageName` kaam nahi karta
* Browser ke **JavaScript Console** me ye code run karo:

```javascript
$A.get("e.force:navigateToURL")
 .setParams({"url": "/apex/pageName"})
 .fire();
```

👉 `pageName` ko apne page ke naam se replace karo

---

## 🧩 Developer Console Features

* **File | Open** → existing Visualforce pages dekh sakte ho
* Double-click → page open
* Apex Classes, Triggers, Components bhi open ho jaate hain

---

## ✨ Add Attributes Using Auto-Suggest

### Attributes Kya Hote Hain?

* Visualforce components ka behavior control karte hain

---

### 🪜 Steps: Attribute Add Karna

1. `<apex:page>` ke opening tag ke andar cursor rakho
2. Space + `s` type karo
   👉 auto-suggest aayega
3. `sidebar` select karo → Enter
4. Value select karo → `false`

```html
<apex:page sidebar="false">
```

5. Save karo

---

### ➕ showHeader Attribute Add Karo

```html
<apex:page sidebar="false" showHeader="false">
    <h1>Hello World</h1>
</apex:page>
```

---

### 🔍 Important Notes

* **Salesforce Classic**:

  * `sidebar` & `showHeader` default = `true`
* **Lightning Experience / Mobile App**:

  * Ye attributes **always false** hote hain
  * Lightning header ko remove nahi kar sakte ❌

---

### 🎨 Salesforce Styling Remove Karna

```html
standardStylesheets="false"
```

👉 Isse Salesforce CSS bhi hat jaati hai

---

## 🧱 Add & Compose Components (Page Structure)

---

### 🔹 apex:pageBlock

* UI ke related items ko group karta hai
* Salesforce Classic style follow karta hai

---

### 🪜 Steps: Page Structure Banana

```html
<apex:page>
    <h1>Hello World</h1>

    <apex:pageBlock title="A Block Title">

        <apex:pageBlockSection title="A Section Title">
            I'm three components deep!
        </apex:pageBlockSection>

    </apex:pageBlock>
</apex:page>
```

---

### 🔹 apex:pageBlockSection

* PageBlock ke andar hota hai
* Collapsible hota hai (user collapse kar sakta hai)

---

### ➕ Add Another Section

```html
<apex:pageBlockSection title="A New Section">
    This is another section.
</apex:pageBlockSection>
```

---

### ⚠️ Rule Yaad Rakho

❌ `<apex:pageBlockSection>`
✔️ Sirf `<apex:pageBlock>` ke andar hi use hota hai

---

## 🚀 Beyond the Basics

* Developer Console = most powerful internal tool
* Dusre options:

  * **Development Mode footer** → quick edits
  * **Setup Editor** → page settings access
  * **VS Code** → professional development

---

### 🧩 Additional Components

* `<apex:pageBlockSectionItem>`
* `<apex:pageBlockButtons>`

👉 **pageBlockSectionItem** nest hota hai
👉 `<apex:pageBlockSection>` ke andar ✅

---

## 📚 Resources

* Salesforce Help: Create Visualforce Pages
* Developer Console Functionality
* Visualforce Developer Guide
* Standard Component Reference
* Salesforce Extensions for VS Code

---

## 🧪 Hands-on Challenge (500 Points)

### 🎯 Task

Ek Visualforce page banao jo **image display kare**

---

### 📌 Requirements

* Page Label: **DisplayImage**
* Page Name: **DisplayImage**
* Standard Salesforce header **hide hona chahiye**
* `<apex:image>` component use karo
* Image URL:

```
https://developer.salesforce.com/files/salesforce-developer-network-logo.png
```

---

### ✅ Final Visualforce Code (Classic)

```html
<apex:page showHeader="false" sidebar="false">
    <apex:image 
        url="https://developer.salesforce.com/files/salesforce-developer-network-logo.png"
        alt="Salesforce Developer Network Logo"/>
</apex:page>
```

---

## 🎉 Summary

* Visualforce = Salesforce ka powerful UI framework
* Tag-based language (HTML jaisa)
* Developer Console = best editor
* Components ko nest karke structured UI banti hai
* Attributes se behavior control hota hai
* Hands-on challenge me `<apex:image>` use hota hai

---

Visualforce expressions ek bahut powerful tool hain jo aapke pages ko **dynamic** banate hain. Matlab ye ki page par wahi dikhega jo user ya system ke status ke hisaab se sahi ho.

Chalo isse step-by-step Hinglish mein samajhte hain:

---

## 1. Visualforce Expression Kya Hai?

Simple words mein, koi bhi aisi cheez jo single value return kare, wo expression hai. Iska syntax hamesha aisa hota hai:
`{! expression }`

* **{! }** ka matlab hai ki Salesforce ko iske andar ki cheez evaluate (calculate) karni hai.
* Ye rendered page par asli value se replace ho jata hai.
* Isme aap variables, formula, aur calculations use kar sakte ho, lekin **method calls (jaise Apex methods) allowed nahi hain.**

---

## 2. Global Variables ($)

Global variables wo hote hain jo Salesforce system ki jaankari dete hain (jaise logged-in user ki details). Inke aage hamesha **$** sign lagta hai.

* **`$User`**: Current user ki details (FirstName, LastName, Email, isActive).
* **`$Organization`**: Aapki company/org ki details.
* **`$Setup`**: Custom settings access karne ke liye.

**Example:**
Agar aapko user ka pehla naam dikhana hai:
`{! $User.FirstName }`

---

## 3. Formula Expressions & Functions

Sirf variables hi nahi, aap Visualforce mein math aur logic bhi laga sakte ho.

* **Concatenation (&)**: Do strings ko jodne ke liye.
`{! $User.FirstName & ' ' & $User.LastName }` (Result: Akshat Jain)
* **Math Functions**: `MAX()`, `SQRT()`, ya `TODAY() + 7` (agle hafte ki date).
* **Text Functions**: `CONTAINS('salesforce.com', 'force.com')` ye check karega ki pehle word mein dusra word hai ya nahi. Iska result **True** ya **False** aata hai.

---

## 4. Conditional Expressions (IF Function)

Ye sabse kaam ki cheez hai. Iska format hota hai:
`{! IF( condition, true_value, false_value ) }`

**Example:**
Agar user active hai toh uska naam dikhao, nahi toh 'inactive' likho:
`{! IF($User.isActive, $User.Username, 'inactive') }`

---

## 5. Hands-on Challenge Guide (DisplayUserInfo)

Aapko ek naya Visualforce page banana hai jisme logged-in user ka `FirstName` dikhana hai.

**Steps:**

1. **Developer Console** kholo.
2. **File | New | Visualforce Page** par click karo.
3. Name rakho: `DisplayUserInfo`.
4. Code mein niche diya gaya markup likho:

```html
<apex:page>
    <apex:pageBlock title="User Info">
        <apex:pageBlockSection columns="1">
            {! $User.FirstName }
        </apex:pageBlockSection>
    </apex:pageBlock>
</apex:page>

```

5. **Save** karo aur **Preview** karke dekho. Challenge pass ho jayega!

---

### Key Points to Remember:

* Expressions **case-insensitive** hote hain (chahe capital likho ya small, farak nahi padta).
* Braces `{! }` ke andar ki **whitespace** (khali jagah) ignore ho jati hai.
* Agar `showHeader="false"` karoge toh Salesforce ka standard header gayab ho jayega (jo humne pichle topic mein discuss kiya tha).

Component,Real-life Example,Visual Role
pageBlock,Ek poora Register,Border aur Page ka title dena.
pageBlockSection,Register ka ek Chapter,Data ko groups mein baantna aur columns decide karna.
Columns,Column lines,Decide karna ki ek line mein kitni fields aayengi.

<apex:page>
    <apex:pageBlock title="User Profile Dashboard">
        
        <apex:pageBlockSection title="User Information" columns="2">
            <apex:outputText label="First Name" value="{!$User.FirstName}"/>
            <apex:outputText label="User ID" value="{!$User.Id}"/>
            
            <apex:outputText label="Last Name" value="{!$User.LastName}"/>
            <apex:outputText label="Manager" value="Jane Doe (Sample)"/>
            
            <apex:outputText label="Email" value="{!$User.Email}"/>
            <apex:outputText label="Department" value="R/D"/>
        </apex:pageBlockSection>
        
        <br/> <apex:pageBlockSection title="System Details" columns="1">
            <apex:outputText label="Is Active" value="{!IF($User.isActive, '✅ True', '❌ False')}"/>
            <apex:outputText label="Last Login" value="{!TODAY()}"/>
            <apex:outputText label="License Type" value="Salesforce User"/>
        </apex:pageBlockSection>
        
    </apex:pageBlock>
</apex:page>

Ab hum **Standard Controllers** ke concept ko Hinglish mein achhe se samajhte hain.

### 1. Standard Controller Kya Hai? (The Logic)

Salesforce mein har object (Account, Contact, etc.) ke paas ek "Standard Controller" pehle se bana-banaya hota hai. Iska kaam hai **Database aur Page ke beech mein bridge (pul)** banana.

* **MVC Model:** Visualforce ek MVC pattern follow karta hai:
* **M (Model):** Aapka database (Account, Contact records).
* **V (View):** Aapka Visualforce page.
* **C (Controller):** Wo logic jo data ko database se utha kar page par lata hai.



---

### 2. Record ID ka Khel (The Request URL)

Agar aap kisi specific record ka data (jaise "Akshat" naam ke contact ki details) dikhana chahte hain, toh Controller ko us record ki **ID** batani padti hai.

* URL mein ID kaise pass karte hain: `.../apex/AccountSummary?id=001D000000JRBes`
* Jab aap URL mein `id=...` dete hain, toh Standard Controller apne aap database mein jaakar us ID ka record dhoond leta hai.

---

### 3. Dot Notation (Traversing Relationships)

Salesforce mein records ek-dusre se jude hote hain.

* **Simple Field:** `{! Account.Name }` (Account ka naam).
* **Related Field:** `{! Account.Owner.Name }`. Yahan hum Account se uske **Owner** (jo ki ek User record hai) par gaye, aur fir Owner ka **Name** uthaya. Isse "Traversing the relationship" kehte hain.

---

### 4. Hands-on Challenge Guide (ContactView)

Aapko ek page banana hai `ContactView` jo Contact ka data dikhaye.

**Requirement:**

1. **First Name** dikhao.
2. **Last Name** dikhao.
3. Contact ke **Owner ka Email** dikhao (ye related field hai).

**Steps:**

1. Developer Console mein jaao.
2. **File | New | Visualforce Page** -> Name: `ContactView`.
3. Niche diya gaya code likho:

```html
<apex:page standardController="Contact">
    <apex:pageBlock title="Contact Details">
        <apex:pageBlockSection columns="1">
            
            First Name: {! Contact.FirstName } <br/>
            Last Name: {! Contact.LastName } <br/>
            
            Owner Email: {! Contact.Owner.Email }
            
        </apex:pageBlockSection>
    </apex:pageBlock>
</apex:page>

```

### Code Breakdown:

* `standardController="Contact"`: Ye line Salesforce ko batati hai ki hum **Contact** object ka data use karne waale hain.
* `{! Contact.FirstName }`: Ye Contact record se First Name nikaal lega.
* `{! Contact.Owner.Email }`: Contact ke Owner (User) ke paas jayega aur uska Email field nikaal kar dikhayega.

**Important Note for Challenge:**
Challenge pass karne ke liye aapko URL mein ID daalne ki zaroorat nahi hai (Salesforce check karte waqt khud ID pass kar deta hai), bas aapka code aur expressions (`{! ... }`) sahi hone chahiye.

Ab hum **Output Components** ke baare mein baat karenge. Inka kaam hota hai data ko dikhana (display karna). Visualforce mein do tarah ke components hote hain: **Coarse-grained** (jo ek baar mein bahut kuch dikhate hain) aur **Fine-grained** (jo specific details ke liye use hote hain).

---

### 1. Coarse-grained Components (The "All-in-One" Tags)

Ye tags kam mehnat mein zyada result dete hain.

* **`<apex:detail>`**: Sirf ye ek line likhne se poora record page (fields, buttons, related lists) ban jata hai.
* **`<apex:relatedList>`**: Agar aapko poora page nahi, sirf ek specific related list (jaise Contacts ya Opportunities) dikhani ho.

---

### 2. Fine-grained Components (The "Custom" Tags)

Jab aapko control chahiye ki kaun sa field kahan dikhe, tab hum inka use karte hain.

* **`<apex:outputField>`**: Ye sirf ek single field ki value dikhata hai. Iski khasiyat ye hai ki ye data type ko pehchanta hai (Currency ko `₹` sign ke saath aur Date ko sahi format mein dikhayega).
* **Dhyan dein:** Agar aap ise bina `<apex:pageBlockSection>` ke use karenge, toh label nahi dikhega. Label dikhane ke liye ise section ke andar daalna zaroori hai.

---

### 3. Iteration Components (Table Banana)

Jab ek se zyada records (List) dikhane hon, toh hum **Iteration** use karte hain. Iska matlab hai ek hi kaam ko baar-baar karna (Loop chalana).

* **`<apex:pageBlockTable>`**: Ye ek sundar table banata hai. Isme do cheezein important hain:
1. **`value`**: Records ki list (Jaise `{!Account.contacts}`).
2. **`var`**: Ek temporary naam jo har row ke liye use hoga (Jaise `contact`).



---

### 4. Hands-on Challenge Guide (OppView)

Aapko ek page banana hai `OppView` jo **Opportunity** ka data dikhaye.

**Requirement:**

1. **Opportunity Name**
2. **Amount**
3. **Close Date**
4. **Account Name** (Relationship field)

**Steps:**

1. Developer Console mein **File | New | Visualforce Page** banayein.
2. Name rakhein: `OppView`.
3. Niche diya gaya code likhein:

```html
<apex:page standardController="Opportunity">
    <apex:pageBlock title="Opportunity Details">
        <apex:pageBlockSection>
            
            <apex:outputField value="{! Opportunity.Name }"/>
            <apex:outputField value="{! Opportunity.Amount }"/>
            <apex:outputField value="{! Opportunity.CloseDate }"/>
            <apex:outputField value="{! Opportunity.Account.Name }"/>
            
        </apex:pageBlockSection>
    </apex:pageBlock>
</apex:page>

```

### Is Code ki Khas Baat:

* **`standardController="Opportunity"`**: Isse page ko pata chala ki hume Opportunity ka data chahiye.
* **`Opportunity.Account.Name`**: Ye humne pichle lesson mein sikha tha (Dot notation). Opportunity se Account par gaye aur uska Name nikaala.
* **`<apex:outputField>`**: Ye Amount ko apne aap Currency format mein dikhayega.

---

### Summary Table:

| Component | Type | Best For |
| --- | --- | --- |
| **apex:detail** | Coarse | Poora page jaldi banane ke liye. |
| **apex:outputField** | Fine | Single field ko design ke saath dikhane ke liye. |
| **apex:pageBlockTable** | Iteration | Records ki list ko table format mein dikhane ke liye. |

Chalo, **`value`** aur **`var`** ke concept ko ek simple diagram aur example se samajhte hain, kyunki ye Visualforce tables ka sabse important hissa hai.

### Table Ka Logic: "The Loop"

Jab hum `<apex:pageBlockTable>` use karte hain, toh ye backend mein ek loop (cycle) chalata hai.

1. **`value="{!Account.contacts}"`**: Ye woh **Pura Thail (Bag)** hai jisme bahut saare Contact records hain.
2. **`var="con"`**: Ye ek **Temporary Khali Dibba** hai.
* Pehli baar loop chalega: Pehla contact is dibbe mein aayega.
* Dusri baar loop chalega: Dusra contact is dibbe mein aayega.


3. **`<apex:column value="{!con.Name}"/>`**: Ye batata hai ki us "dibbe" mein se kaunsi cheez nikaal kar column mein dikhani hai.

---

### Ek Real-Life Example se Samjho:

Maan lo ek Class (Account) hai jisme bahut saare Students (Contacts) hain.

* **Value:** Poori class ke students ki list.
* **Var:** Ek-ek karke student ko stage par bulana. Jab "Rahul" stage par aayega, toh `con` ka matlab Rahul hoga. Jab "Sneha" aayegi, toh `con` ka matlab Sneha hoga.

```html
<apex:pageBlockTable value="{!Account.contacts}" var="con">
    
    <apex:column value="{!con.FirstName}"/> 
    <apex:column value="{!con.LastName}"/>
    
</apex:pageBlockTable>

```

### Table vs List:

| Feature | `<apex:pageBlockTable>` | `<apex:relatedList>` |
| --- | --- | --- |
| **Control** | Aap decide karte ho kaunsa column dikhega. | Salesforce decide karta hai. |
| **Design** | Classic look and feel. | Poora standard layout. |
| **Customization** | Aap formula ya icons bhi daal sakte ho. | Fixed format hota hai. |

Ab tak humne sirf data ko **dekhna** (Output) sikha hai. Ab hum sikhenge ki data ko **enter aur save** (Input) kaise karte hain. Salesforce mein forms banane ke liye `<apex:form>` ka hona sabse zaroori hai.

---

### 1. Visualforce Form ke 3 Main Pillars

Ek functional form banane ke liye ye 3 tags milkar kaam karte hain:

1. **`<apex:form>`**: Ye ek boundary hai. Iske andar jo bhi data hoga, wahi server par "Save" hone ke liye jayega.
2. **`<apex:inputField>`**: Ye data entry ke liye box banata hai. Iski sabse badi khoobi ye hai ki ye field ke **Data Type** ke hisaab se khud ko badal leta hai (Jaise Date field ke liye Calendar dikhana).
3. **`<apex:commandButton>`**: Ye wo button hai jo "Save" ya "Cancel" jaisi actions ko trigger karta hai.

---

### 2. Validation aur Errors (`<apex:pageMessages>`)

Jab user galat data bharta hai (jaise Email mein `@` nahi lagaya), toh Salesforce error generate karta hai. In errors ko page par dikhane ke liye hum `<apex:pageMessages/>` tag use karte hain. Ise hamesha form ke top par rakhna chahiye.

---

### 3. Global Actions (`$Action`)

Aap Related Lists mein "Edit" ya "Delete" ke links bhi de sakte ho bina kisi lamba code likhe.
`{! URLFOR($Action.Contact.Edit, contact.Id) }`
Ye formula apne aap us contact ke standard edit page ka rasta (URL) bana deta hai.

---

### 4. Hands-on Challenge Guide (CreateContact)

Aapko ek page banana hai `CreateContact` jo ek naya Contact record insert kare.

**Requirement:**

1. **Standard Controller:** Contact.
2. **Form** hona chahiye.
3. **Fields:** First Name, Last Name, Email.
4. **Save Button** hona chahiye jo record save karke uske detail page par le jaye.

**Steps:**

1. Developer Console mein **File | New | Visualforce Page** banayein.
2. Name: `CreateContact`.
3. Niche diya gaya code paste karein:

```html
<apex:page standardController="Contact">
    <apex:form>
        <apex:pageBlock title="Create Contact">
            
            <apex:pageMessages/>
            
            <apex:pageBlockSection columns="1">
                <apex:inputField value="{! Contact.FirstName }"/>
                <apex:inputField value="{! Contact.LastName }"/>
                <apex:inputField value="{! Contact.Email }"/>
            </apex:pageBlockSection>
            
            <apex:pageBlockButtons>
                <apex:commandButton action="{! save }" value="Save" />
            </apex:pageBlockButtons>
            
        </apex:pageBlock>
    </apex:form>
</apex:page>

```

### Is Challenge ko Samajhte Hain:

* **`action="{! save }"`**: Ye Standard Controller ka built-in method hai. Jab aap ise click karenge, Salesforce data valid check karega aur save hone ke baad automatic us naye contact ke page par redirect kar dega.
* **`columns="1"`**: Maine use kiya taaki form mobile-friendly aur saaf-suthra dikhe.

**Ek Choti Tip:** Trailhead challenge check karne se pehle "Save" button par click karke dekhna ki kya naya contact ban raha hai. Agar naya page khul gaya, matlab aapka code 100% sahi hai!

Aapke notes banane ke liye ye raha **Standard List Controller** ka poora nichod (Summary). Isme saari important technical details aur logic ko Hinglish mein simplify kiya gaya hai.

---

## 📘 Visualforce Standard List Controller Notes

### 1. Introduction: Standard vs Standard List Controller

* **Standard Controller:** Ek waqt mein sirf **ek record** (single record) par kaam karta hai.
* **Standard List Controller:** Ye ek saath **multiple records** (collection of records) par kaam karne ke liye use hota hai.
* **Key Attribute:** Isme `recordSetVar` attribute ka use hota hai, jo records ki list ko ek variable mein store karta hai.

---

### 2. Displaying a List (Iteration Components)

Jab hamare paas records ki list hoti hai, toh hume unhe loop mein dikhane ke liye **Iteration Components** ki zaroorat hoti hai.

* **`<apex:pageBlockTable>`:** Ye platform-styled table banata hai.
* **Attributes:**
* `value="{! contacts }"`: Ye wo list hai jo `recordSetVar` se aayi hai.
* `var="ct"`: Ye ek temporary variable hai jo loop ke har ek record ko represent karta hai.



**Example Code:**

```html
<apex:page standardController="Contact" recordSetVar="contacts">
    <apex:pageBlock title="Contacts List">
        <apex:pageBlockTable value="{! contacts }" var="ct">
            <apex:column value="{! ct.FirstName }"/>
            <apex:column value="{! ct.LastName }"/>
        </apex:pageBlockTable>
    </apex:pageBlock>
</apex:page>

```

---

### 3. List View Filtering (Ajax Magic)

Aap predefined list views (jaise "All Contacts", "Recently Viewed") ko page par filter ki tarah use kar sakte hain.

* **`listViewOptions`:** Ye property saare available filters ki list deti hai.
* **`filterId`:** Isse hum select kiya gaya filter set karte hain.
* **`<apex:actionSupport>`:** Iska use karke hum **Ajax** effect create karte hain. `reRender` attribute ki wajah se poora page refresh nahi hota, sirf specified component update hota hai.

---

### 4. Pagination (Navigating Records)

By default, list controller **20 records** dikhata hai. Baaki records dekhne ke liye pagination zaroori hai.

**Important Actions & Properties:**
| Action/Property | Description |
| :--- | :--- |
| `{! Next }` | Agle page par jaane ke liye (Action). |
| `{! Previous }` | Pichle page par jaane ke liye (Action). |
| `{! HasNext }` | Check karta hai ki aage records hain ya nahi (Boolean). |
| `{! HasPrevious }` | Check karta hai ki peeche records hain ya nahi (Boolean). |
| `{! PageNumber }` | Current page number batata hai. |

**Logic for Page Indicator:**
`Page: {!PageNumber} of {! CEILING(ResultSize / PageSize) }`
*(Yahan CEILING function total pages calculate karne ke liye use hota hai taaki decimal value na aaye.)*

---

### 5. Hands-on Challenge Solution (Logic)

Challenge mein aapko `apex:repeat` use karna hai. Ye ek "raw" iteration component hai jo koi styling apply nahi karta, sirf content ko repeat karta hai.

**Step-by-Step Code for Challenge:**

1. **Page Name:** `AccountList`
2. **Controller:** `Account`
3. **Variable:** `accounts`
4. **Link Logic:** `/{!a.id}` use karke record page par redirect karte hain.

```html
<apex:page standardController="Account" recordSetVar="accounts">
    <apex:pageBlock title="Accounts List">
        <ul>
            <apex:repeat value="{! accounts }" var="a">
                <li>
                    <apex:outputLink value="/{! a.Id }">
                        {! a.Name }
                    </apex:outputLink>
                </li>
            </apex:repeat>
        </ul>
    </apex:pageBlock>
</apex:page>

```

---

### 💡 Pro Tips for Exam/Interview:

* **Sorting:** Visualforce markup se sorting directly nahi hoti, uske liye Custom Apex code (StandardSetController) chahiye hota hai.
* **StandardSetController:** Back-end par Standard List Controller isi Apex class ka use karta hai.
* **Pagination Limits:** Large data sets ke liye pagination user experience behtar banata hai.


Salesforce mein **Static Resources** ka matlab hai ek aisi jagah jahan aap apni files (jaise images, CSS, JavaScript, ya Zip folders) ko upload karke rakh dete ho taaki unhe Visualforce pages par use kiya ja sake.

Iska sabse bada fayda ye hai ki Salesforce ise ek **CDN (Content Delivery Network)** ki tarah handle karta hai, jis se aapka page jaldi load hota hai.

---

## 1. Static Resources Kyu Use Karein? (Why use them?)

Socho aap ek project bana rahe ho jisme aapko 50 alag-alag icons aur ek badi CSS file chahiye. Agar aap har baar code mein link daloge toh manage karna mushkil hoga.

* **Centralized:** Ek hi jagah saari files rahengi.
* **Caching:** Browser in files ko yaad rakhta hai, baar-baar download nahi karta.
* **Version Control:** Agar aap jQuery file update karte ho, toh bas Static Resource badal do, saare 100 pages par khud change ho jayega.

---

## 2. Individual vs Zipped Resources

Real-world project mein hum dono use karte hain:

* **Individual:** Jab sirf ek single file ho (jaise `logo.png` ya `jquery.js`). Isse direct `$Resource.FileName` se access karte hain.
* **Zipped (.zip):** Jab bahut saari related files hon (jaise ek pura theme jisme images, fonts, aur CSS ho). Isse access karne ke liye `URLFOR()` function ka use hota hai.

---

## 3. Visualforce Page mein kaise add karein?

Iske liye hum `$Resource` variable use karte hain:

* **JavaScript ke liye:** `<apex:includeScript value="{!$Resource.MyJSFile}"/>`
* **CSS ke liye:** `<apex:stylesheet value="{!$Resource.MyCSSFile}"/>`
* **Zip ke andar ki file ke liye:** `{!URLFOR($Resource.ZipName, 'folder/image.png')}`

---

## 4. Hands-on Challenge Guide (Step-by-Step)

Aapko ek challenge mila hai jisme ek image dikhani hai. Isse aise solve karein:

### Step 1: File Download Karein

Pehle challenge mein di gayi [zip file](https://www.google.com/search?q=https://developer.salesforce.com/projects/assets/lightning-platform-dev-intermediate/cats.zip) download karein. Is zip ke andar ek `cats` naam ka folder hai aur usme `kitten1.jpg` hai.

### Step 2: Static Resource Upload Karein

1. Salesforce **Setup** mein jayein.
2. Quick Find mein **Static Resources** search karein.
3. **New** par click karein.
4. **Name:** `vfimagetest` (Dhyan rahe, naam exact yahi hona chahiye).
5. **File:** Jo zip download ki thi use select karein.
6. **Cache Control:** Public (agar dikhe toh).
7. **Save** kar dein.

### Step 3: Visualforce Page Banayein

1. Developer Console kholein (**File > New > Visualforce Page**).
2. Page ka naam rakhein: `ShowImage`.
3. Niche diya gaya code copy-paste karein:

```html
<apex:page >
    <apex:image url="{!URLFOR($Resource.vfimagetest, 'cats/kitten1.jpg')}"/>
</apex:page>

```

### Explanation of Code:

* `$Resource.vfimagetest`: Ye hamari zip file ko refer kar raha hai.
* `'cats/kitten1.jpg'`: Ye zip file ke andar ka sahi rasta (path) hai.
* `URLFOR()`: Ye function zip file ke andar jhaankne (access karne) mein madad karta hai.

Bilkul! Aapke prompt ke har ek point ko detail mein, production-level depth ke saath aur simple Hinglish mein explain karte hain. Yeh notes aapke interview aur real projects dono mein kaam aayenge.

---

## 1. Custom Controller: Kya aur Kyu? (The "Why")

Standard Controller wahi kaam karta hai jo Salesforce ne pehle se fix kiya hai. Lekin real-world projects mein requirements complex hoti hain.

* **Definition:** Ek Apex class jo Visualforce page ke logic ko control karti hai use **Custom Controller** kehte hain.
* **Kyu use karein?**
* Jab aapko **multiple objects** ka data ek hi page par dikhana ho (Standard controller sirf ek object handle karta hai).
* Jab aapko **Custom Logic** lagana ho (jaise kisi button click par email bhejna ya data calculate karna).
* Jab aapko **External API** se data lekar page par dikhana ho.


* **Key Attribute:** Isse `<apex:page controller="MyControllerName">` se link kiya jata hai.

---

## 2. Controller ke Main Parts (Key Attributes)

Custom Controller likhte waqt 3 cheezein sabse important hoti hain:

### A. Getter Methods (Data Page par lane ke liye)

Visualforce page direct database se baat nahi kar sakta. Woh controller se poochta hai.

* **Rule:** Method ka naam `get` se shuru hona chahiye (e.g., `getContacts`).
* **VF Access:** Page par hum sirf `{! contacts }` likhte hain. Visualforce apne aap `get` prefix ko pehchaan leta hai.

### B. Action Methods (Buttons/Links ke liye)

Jab user kisi button par click karta hai, toh jo logic execute hota hai use Action Method kehte hain.

* **Example:** `sortByFirstName()`, `saveData()`.
* **Return Type:** Yeh ya toh `void` hote hain (agar usi page par rehna ho) ya `PageReference` (agar dusre page par redirect karna ho).

### C. Properties (`{ get; set; }`) - Production Standard

Aaj kal getters/setters alag se likhne ke bajaye hum **Properties** use karte hain:
`public List<Contact> contactList { get; set; }`
Yeh ek hi line mein variable bhi bana deta hai aur use page par accessible bhi kar deta hai.

---

---

## 3. Real-World Project: Sorting Example (Step-by-Step)

Maan lo aapko ek list dikhani hai jise user column par click karke sort kar sake.

### Apex Controller Logic:

1. Ek variable banaya `sortOrder` jo default mein 'LastName' hai.
2. `getContacts()` method mein query likhi jisme `ORDER BY :sortOrder` use kiya.
3. Do action methods banaye jo `sortOrder` ki value badalte hain.

### Visualforce Page UI:

1. `<apex:commandLink>` use kiya column header mein.
2. `reRender="tableId"` use kiya taaki poora page refresh na ho, sirf table update ho. (Ise **AJAX** functionality kehte hain).

---

## 4. Hands-on Challenge Guide (Step-by-Step)

Hamein ek page banana hai jo "New" status waale Cases dikhaye aur unka clickable link de.

### Step 1: Controller Banayein (`NewCaseListController`)

1. Developer Console > File > New > Apex Class.
2. Naam: `NewCaseListController`.

```java
public class NewCaseListController {
    // 1. Public method jo List return karega
    public List<Case> getNewCases() {
        // 2. SOQL query filter ke saath (Status = 'New')
        List<Case> results = [SELECT Id, CaseNumber FROM Case WHERE Status = 'New'];
        
        // 3. Data return kar rahe hain
        return results;
    }
}

```

### Step 2: Visualforce Page Banayein (`NewCaseList`)

1. Developer Console > File > New > Visualforce Page.
2. Naam: `NewCaseList`.

```html
<apex:page controller="NewCaseListController">
    <apex:pageBlock title="New Cases List">
        <ul>
            <apex:repeat value="{! newCases }" var="case">
                <li>
                    <apex:outputLink value="/{! case.Id }">
                        {! case.CaseNumber }
                    </apex:outputLink>
                </li>
            </apex:repeat>
        </ul>
    </apex:pageBlock>
</apex:page>

```

---

## 5. Notes for Production & Interview

* **Sharing Keywords:** Production mein hamesha `public with sharing class` ya `without sharing` likhein. `with sharing` likhne se user wahi data dekh payega jiska uske paas access hai (Security Best Practice).
* **Security (SOQL Injection):** Kabhi bhi query mein string concatenate na karein. Hamesha bind variables (`:variableName`) use karein.
* **View State:** Custom controller ka saara data "View State" mein store hota hai jo 135KB tak ho sakta hai. Isliye unnecessary data store na karein, `transient` keyword ka use karein agar data refresh par nahi chahiye.
* **Testing:** Production mein bhejne se pehle aapko is controller ke liye **Test Class** likhni hogi jisme kam se kam 75% code coverage ho.

---

### Summary Checklist for Notes:

1. **Controller Attribute:** Page ko class se link karta hai.
2. **Getter Pattern:** `getSomething()` in Apex = `{! something }` in VF.
3. **Repeat Component:** List of records ko iterate (loop) karne ke liye.
4. **OutputLink:** Dynamic navigation/URL banane ke liye.



